#!/bin/bash
[ -f ~/.minttyrc ] && diff -swu ~/bin/myminttyrc ~/.minttyrc
[ -f ~/.vimrc    ] && diff -swu ~/bin/myvimrc    ~/.vimrc
gitignore=$(git config --get core.excludesfile)
[ -f $gitignore  ] && diff -swu ~/bin/mygitignore $gitignore
